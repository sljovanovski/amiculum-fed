# amiculum-fed

To run localy
```
$ yarn && yarn start
```

To run production
```
$ yarn && yarn build
```
* After building production version you only need these folders and files to run the website:
    - `static`
    - `vendor`
    - `index.html`
